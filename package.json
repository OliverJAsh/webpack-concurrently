{
  "name": "webpack-concurrently",
  "private": true,
  "scripts": {
    "compile": "concurrently --names client,server 'webpack --progress --config config/webpack.client.config.js' 'webpack --progress --config config/webpack.server.config.js'"
  },
  "dependencies": {
    "fp-ts": "^2.6.2"
  },
  "devDependencies": {
    "concurrently": "^5.2.0",
    "webpack": "^4.43.0",
    "webpack-cli": "^3.3.11"
  }
}
